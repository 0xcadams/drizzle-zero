{
  "name": "@zero-drizzle/integration",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "zero-drizzle:generate": "zero-drizzle generate -f --disable-legacy-mutators --disable-legacy-queries",
    "pretest": "pnpm zero-drizzle:generate",
    "test": "pnpm build && vitest run"
  },
  "dependencies": {
    "@zero-drizzle/custom-types": "workspace:*",
    "@zero-drizzle/db": "workspace:*",
    "@hono/node-server": "^1.19.6",
    "zero-drizzle": "workspace:*",
    "hono": "^4.10.7",
    "zod": "^4.1.13"
  }
}
